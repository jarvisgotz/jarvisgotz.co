# Hero Section Tweaks

Two small edits to the homepage hero (`src/routes/index.tsx`):

1. **Remove the gradient fade-out** on the hero image — delete the `bg-gradient-to-t` overlay div.
2. **Center and whiten the text block** — switch the text container from bottom-left alignment to centered, and change the text color from the default dark to white so it reads clearly against the full-bleed photo.

No other pages or components touched.